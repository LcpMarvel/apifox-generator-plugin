{
  "mcpServers": {
    "apifox": {
      "command": "npx",
      "args": [
        "-y",
        "apifox-mcp-server@latest",
        "--project=YOUR_PROJECT_ID_HERE"
      ],
      "env": {
        "APIFOX_ACCESS_TOKEN": "${APIFOX_ACCESS_TOKEN}"
      }
    }
  }
}
